# julietsabol.com

A single static page (`index.html`) served by GitHub Pages from the root of the `master` branch. The `CNAME` file points it at julietsabol.com.

To make a change, edit `index.html` and push to `master`. GitHub Pages redeploys it automatically.
